namespace BF.Game.Runtime.Battle.Commands
{
    /// <summary>
    /// 触发器结算协作者（预留扩展点）。
    /// 本期只提供空实现，为后续 on-hit、on-damaged、on-kill 等触发链预留位置。
    /// </summary>
    public class BFTriggerResolver
    {
        // 预留：处理 on-hit、on-damaged、on-kill、on-death-finished 等触发链
    }
}
